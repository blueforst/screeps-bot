## 1. 手动任务控制

- [x] 1.1 定义手动 `upgrader` 任务的 Memory 数据结构和启动、停止、状态接口。
- [x] 1.2 将自动 hub 升级控制替换为手动任务驱动的 RCL7 配置创建与终止清理。
- [x] 1.3 实现本地 T3 充足性判定及 boost 实验室准备或释放。

## 2. 角色迁移

- [x] 2.1 将专用角色、出生档案、注册和优先级从 `hubUpgrader` 迁移到 `upgrader`。
- [x] 2.2 使角色根据自身手动任务和配置验证 controller，而非 hub 配置。

## 3. 验证

- [x] 3.1 覆盖手动发布、停止、RCL8 自动终止以及 T3 足量/不足的运行时测试。
- [x] 3.2 运行目标测试、TypeScript 检查、构建和完整测试套件。
