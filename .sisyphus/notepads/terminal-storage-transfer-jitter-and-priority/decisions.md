# Decisions

## Task 1: Terminal energy 25k floor
- Use local helper `getProtectedTerminalEnergy(room, snapshots)` for reusability
- Energy counts toward 250k terminal total capacity
- `terminalEnergyReserve` stays at 20_000 default
